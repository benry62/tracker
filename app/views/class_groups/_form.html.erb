<%= form_with(model: class_group, local: true) do |form| %>
  <% if class_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(class_group.errors.count, "error") %> prohibited this class_group from being saved:</h2>

      <ul>
      <% class_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :class_group_name %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :class_group_year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
