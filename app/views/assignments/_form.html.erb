

<%= form_with(model: assignment, local: true) do |form| %>


<p> <%= form.collection_select(:test_id, Test.all, :id, :name, prompt: true ) %></p>


  <%= form.collection_check_boxes(:class_group_id, ClassGroup.order(['year', 'name']),  :id, :name) do |b| %>
     <%= b.check_box %>
     <%= b.label %>
  <% end %>



  <div class="actions">
    <%= form.submit %>
  </div>


<% end %>
